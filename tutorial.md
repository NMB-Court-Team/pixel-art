# 使用教程

本教程每页由二级标题（`##`）划分，页面内容在打开网页时自动读取并转换成可翻页弹窗。

## 0 · 说明

像素画用于色盲派对小游戏的地板，受游戏限制：
  - 画布固定 48×48，不可自定义
  - 颜色最多 16 色，超出自动合并
  - 颜色过近会自动合并
  - 单色像素块过少会自动合并

如果网站使用不便，可用「导入图片」导入已画好的图，系统会按规则自动调整

## 导入与导出

- 「导入图片」导入图片并预览，自动按规则调整颜色
- 导出 PNG 若画面存在透明像素且没有选背景色，会拒绝导出
- 「从文本导入」可粘贴「导出文本」生成的纯文本
- 「导出 PNG」导出后可点「跳转提交」打开提交地址

## 颜色与调色板

- 拖动选点预览颜色；点击按钮或**Shift + 单击**把候选色加入调色板，点击按钮或**Alt + 单击**更新选中颜色
- 加入调色盘的颜色会在周围画出限制范围，范围内的颜色不能再选择
- 删除选中颜色会把画布上该颜色的像素变为透明并用选区选中所有删除的格子
- 设置背景色会把背景变成该颜色，可以点击画布上方的「背景」开关显示，背景颜色会在导出时自动填充透明像素
- 「最小色差」限制任意两色的视觉差距；「颜色上限」控制最多可用颜色数；「最少色块」保证每种颜色占够格子数，可以点「整理」自动合并
- 「清空色板」会删除调色板所有颜色和画布上的色块（需二次确认，可撤销）
- 如果没有特殊需求，建议保留默认设置

## 基础绘制

- 顶部选「画笔」或「橡皮」，在二级面板里切换圆形 / 方形笔尖与 `0～8 px` 半径
- 按住 **Shift** 点击可从上一笔终点继续画直线
- 快捷键：按住 **Alt** 在画布上临时取色；**B** 画笔、**E** 橡皮、**F** 填充、**M** 选区、**G** 网格
- 画面会自动保存到本地，**Ctrl/Cmd + Z** 撤销、**Ctrl/Cmd + Y** 重做
- 填充工具支持「邻接填充」与「选区填充」两种模式

## 图形工具

- 点击工具栏的「图形」，在二级面板选择 **矩形 / 空心矩形 / 圆形 / 空心圆形 / 直线**
- 按住 **Shift** 拖动可临时反转空心 / 实心（例如实心矩形会变成空心矩形）
- 绘制的图形沿用当前颜色与对称设置

## 选区

- 选区模式：矩形、手涂、**魔棒**（相连的同色区域）、**同色选择**（整幅同色，透明背景也能一键全选）
- 按住 **Shift** 点击可把新范围并入现有选区
- 配合「复制 / 剪切 / 移动 / 粘贴」搬运图案，**Delete** 删除选区内像素，**Esc** 取消选区

## 视图与画布

- **G** 或「网格」切换网格，导入图片界面与主界面同步
- 「背景」按钮（在「网格」左侧，默认开启）切换画布底层显示**背景色**还是透明棋盘格；它只影响显示
- 滚轮缩放；右键拖动画布平移，触屏用双指缩放与移动
- 「重置视角」让画布居中铺满；「清空」会二次确认后清空整幅画面（不会清空调色板）

