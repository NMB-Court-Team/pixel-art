# 使用教程

本教程每页由二级标题（`##`）划分，页面内容在打开网页时自动读取并转换成可翻页弹窗。

## 0 · 说明

- 像素画是为了色盲派对小游戏制作地板，会有一些服务于游戏的限制：
- - 画布大小固定为 48x48，不支持自定义尺寸。
- - 颜色数量限制为 16 色，超过会自动合并。
- - 颜色不能太接近，否则会自动合并。
- - 单种颜色的像素块不能太少，否则会自动合并。
- 如果你觉得网站使用不顺手，可以使用「导入图片」功能导入绘制好的图片，会自动适配规则进行调整。

## 1 · 基础绘制

- 顶部选「画笔」或「橡皮」，在二级面板里切换圆形 / 方形笔尖与 `0～8 px` 半径。
- 画笔完成一笔后，按住 **Shift** 点击可从上一笔终点继续画直线。
- 按住 **Alt** 临时取色；快捷键：**B** 画笔、**E** 橡皮、**F** 填充、**M** 选区、**G** 网格。
- 填充工具支持「邻接填充」与「选区填充」两种模式。

## 2 · 图形工具

- 点击工具栏的「图形」，在二级面板选择 **矩形 / 空心矩形 / 圆形 / 空心圆形 / 直线**。
- 按住 **Shift** 拖动可临时反转空心 / 实心（例如实心矩形会变成空心矩形）。
- 绘制的图形沿用当前颜色、笔刷规则与对称设置。

## 3 · 颜色与调色板

- 右侧颜色工作室有 **色相环 / Lab 色域 / 通道** 三种视图，随时切换不会改变已选颜色。
- 拖动色图只预览；**Shift + 单击**把候选色加入调色板，**Alt + 单击**更新选中颜色。
- 「最小色差」限制任意两色的视觉差距；「颜色上限」控制最多可用颜色数；「最少色块」保证每种颜色占够格子数，不满足时可以点「整理」自动合并。
- 「清空」调色板会删除所有颜色和画布上的色块

## 4 · 选区

- 选区模式：矩形、手涂、**魔棒**（相连的同色区域）、**同色选择**（整幅同色，透明背景也能一键全选）。
- 按住 **Shift** 点击可把新范围并入现有选区。
- 配合「复制 / 剪切 / 移动 / 粘贴」搬运图案，**Delete** 删除选区内像素，**Esc** 取消选区。

## 5 · 视图与画布

- **G** 或「网格」切换网格（含中心十字，导入图片预览同步显示）。
- 滚轮按 10% 步进缩放（20%～800%）；右键拖动画布平移，触屏用双指缩放与移动。
- 「重置视角」让画布居中铺满；「清空」会二次确认后清空整幅画面。

## 6 · 导入与导出

- 「导入图片」可定位、缩放图片并实时预览量化结果（默认使用「颜色偏移恢复」）。
- 「从配置导入」可粘贴 `floor_patterns` 配置还原画面。
- 「导出 PNG」默认 1×（按住 Alt 为 16×），输出索引色 PNG；导出后可点「跳转提交」打开提交地址。
- 画面会自动保存到本地，**Ctrl/Cmd + Z** 撤销、**Ctrl/Cmd + Y** 重做。
